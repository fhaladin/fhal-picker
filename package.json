{
  "name": "fhal-picker",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile-style": "sass src/style/fhal-picker.scss dist/css/fhal-picker.css --style compressed",
    "build": "rm -rf dist && babel src -d dist && babel-minify dist/fhal-picker.js --out-file dist/fhal-picker.min.js --mangle.keepClassName && compile-style"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-minify": "^0.5.2",
    "babel-preset-es2015": "^6.24.1"
  }
}
